#include <map>
#include <iostream>

constexpr unsigned int MOD = 4294967161;

int main()
{
    std::map<std::pair<unsigned int, unsigned int>, unsigned int> mtx;

    unsigned int r, c, v;

    while (std::cin >> r) {
        std::cin >> c >> v;
    }
}